sudo apt update
sudo apt install python3-venv
git clone XXXXXXX
cd XXXXXXXXXXX
python3 -m venv venv
source venv/bin/activate
pip install --upgrade  pip
pip install -r requirements.txt




python3 -m venv venv && source venv/bin/activate && pip install --upgrade  pip


sudo apt install python3 && sudo apt install python3-pip && pip3 install --upgrade pip && pip3 install --upgrade pip && sudo apt install python3-venv && python3 -m venv venv && source venv/bin/activate
